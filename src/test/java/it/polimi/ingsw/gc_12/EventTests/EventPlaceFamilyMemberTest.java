package it.polimi.ingsw.gc_12.EventTests;

import org.junit.Test;

public class EventPlaceFamilyMemberTest {

    @Test
    public void test(){
        /*
        Player player = mock(Player.class);
        List<Occupiable> occupiables = new ArrayList<>();
        FamilyMember familyMember = mock(FamilyMember.class);
        String description = "event test";

        EventPlaceFamilyMember event = new EventPlaceFamilyMember(occupiables, familyMember);
        assertEquals(null, event.getFamilyMember());
        assertEquals("EventPlaceFamilyMember", event.toString());

        event = new EventPlaceFamilyMember(occupiables, familyMember, description);
        assertEquals(description, event.toString());
        assertEquals(occupiables, event.getOccupiables());
        */
    }
}
